insertUser:
INSERT INTO user_model (subscription_key, device_id, created_date)
VALUES (?, ?, ?);

selectAllUsers:
SELECT * FROM user_model;

selectAllUsersBySubscriptionKey:
SELECT * FROM user_model
WHERE subscription_key = ?;